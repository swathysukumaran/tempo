import React from "react";
import { useEffect, useState } from "react";
function GeocodedMarker({ name, address }: { name: string; address: string }) {
  return <div></div>;
}

export default GeocodedMarker;
